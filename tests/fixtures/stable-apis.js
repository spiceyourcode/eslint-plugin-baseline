// Stable APIs - should pass linting
const stableArray = [1, 2, 3];
stableArray.sort();

const stableObject = { a: 1, b: 2 };
Object.keys(stableObject);

const jsonString = '{"key":"value"}';
JSON.parse(jsonString);

const arr = [10, 20, 30];
arr.at(1);
